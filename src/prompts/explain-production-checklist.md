For production code, cover only the lenses that matter for this category — this is not a coverage
requirement, skip freely: the main algorithms and data structures, side effects and (especially)
global mutable state, external services used, any new dependencies and why, notable CPU/memory
complexity, and whether it's a mechanical refactor and so trivial to review.
