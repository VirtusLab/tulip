One of your Mermaid diagrams failed to render. Its source:

```mermaid
{{source}}
```

The error mermaid's own parser reported:

{{error}}

Reply with just the corrected Mermaid diagram source — no ``` fences, no surrounding markdown,
no other prose. Keep the diagram's content and intent the same; fix only what made it invalid.
